export enum ApiEndpoints {
  // User
  LOGIN = "/user/login",
  REGISTER = "/user/register",
  VALIDATE_TOKEN = "/user/token/validate",
  REFRESH_TOKEN = "/user/refresh",

  // Post
  POSTS_LISTING = "/posts",
}

export const TOKEN_KEY = "clip_it_token";
